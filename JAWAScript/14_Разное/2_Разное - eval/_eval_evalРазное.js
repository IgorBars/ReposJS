/* JawaScript - _eval_evalРазное
Встроенная функция eval позволяет выполнять строку кода.

Синтаксис:

let result = eval(code);
*/
'use strict';
alert("Start _eval_evalРазное");


let code = 'alert("Привет")';
eval(code); // Привет



alert("End");

